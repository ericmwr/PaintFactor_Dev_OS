You are operating inside the PaintFactor DevOS.

This is a DEVELOPMENT-TIME spec generation task.
You are NOT estimating a real job.

---

## SPEC TO GENERATE

Spec Title:
SF_TRIM_NC_PAINT - Baseboard, Window/Door Casing

Optional Clarifier 
When Pre Primed Trim installed on New construction all trim same color and sheen.  Full Prep
---

## SYSTEM CONSTRAINTS (MANDATORY)

1. PaintScope is the sole source of geometry.
    - Do NOT invent or compute SF, LF, EA internally.
    - All quantities must map to PaintScope catalog keys.
2. You must follow:
    - docs/PaintFactor_OS.md
    - docs/Materials_and_Consumables_Doctrine.md
    - docs/Estimation_Modifiers_Doctrine.md
    - docs/Conventions.md
    - docs/PaintScope_Adjacency_Schema.md
    - docs/Protection_and_Masking_Doctrine.md
    - docs/Quality_Tiers_and_Surface_Condition.md
    - docs/PaintScope_EdgeLF_Mapping.md
    - docs/PaintScope_Quantity_Key_Catalog.md
    - docs/PaintScope_Asset_Catalog.md
    - docs/Spec_Input_to_PaintScope_Key_Mapping.md
3. This spec must be:
    - Geometry-driven
    - Adjacency-aware
    - Auditable
    - Data-first (not procedural instructions)

---

## WORKFLOW (DO NOT SKIP STEPS)

### STEP 0 — PaintScope Readiness Gate (Required)

- Identify all required PaintScope quantity keys for this spec.
- If ANY required key does not exist in the catalog:
    - STOP
    - Output missing_paintscope_keys[]
    - Propose new keys (do NOT invent values)
    - Wait for human approval before proceeding

### STEP 1 — Research (Spec Researcher)

- Research professional repaint practices for the specified substrate.
- Identify:
    - Condition drivers
    - Application method impacts
    - Adjacency implications
    - Failure modes
- Output:
    - required_paintscope_keys[]
    - adjacency_notes[]
    - research_summary (concise)

### STEP 2 — SOP Module Design (SOP Librarian)

- Create LEGO-style SOP modules composed of atomic tasks.
- Rules:
    - One action per task
    - Explicit UOM per task (SF, LF, EA)
    - Repeated work uses explicit rounds (e.g., SAND_R1, SAND_R2)
    - Any edge work MUST require EdgeLF inputs
- No geometry computation.

### STEP 3 — Materials System (Materials Manager)

- Define:
    - Coating systems
    - Protection materials
    - Application consumables
- Rules:
    - Quantities must reference PaintScope keys
    - No “per room” or narrative quantities
    - Manual capture only if explicitly required and fully defined

### STEP 4 — Production Logic (Estimation Engineer)

- Define:
    - Baseline production rates (per UOM)
    - Fixed-time tasks where applicable
    - Modifier behavior - 
- Rules:
    - Modifiers increase TIME, not production rate
    - No wildcard task references unless engine-supported

### STEP 5 — Spec Assembly (SpecFactory)

- Assemble:
    - spec.json
    - sop_modules.json
    - materials.json
    - production.json
- Ensure all required inputs are declared with:
    - input_name
    - uom
    - paintscope_key

### STEP 6 — QA & Doctrine Enforcement (System Critic)

- FAIL the spec if:
    - Geometry is computed internally
    - Edge work lacks EdgeLF
    - Assets are referenced without catalog keys
    - Required PaintScope keys are missing
- Output qa_report.json with doctrine_checks[].

---

## OUTPUT RULES

- Write files ONLY after explicit approval.
- If approval is denied and feedback is provided:
    - Re-run the affected agent(s)
    - Incorporate feedback
    - Re-present for approval

---

## SUCCESS CRITERIA

This spec is successful if:

- It can be fully evaluated using only PaintScope quantities
- It introduces no new logic at runtime
- Every task, material, and rate is traceable to a measurable input

Proceed.




