One thing to tighten up in Fix 1:
The QA Critic instruction says "Validate resolution.json against raw registries." That's correct — the Critic is the one agent that should still load raw registries to verify the Resolver didn't make mistakes. Worth making that explicit: the Critic loads both resolution.json AND the raw registries, while all other downstream agents load only resolution.json.
One thing missing from Fix 6:
Rules 9 and 10 cover TSK_ prefix and ITM_ ID honoring. There's no rule covering the enum filtering — something like: "Downstream agents MUST use only enum values present in resolution.json → applicable_enums. If a value is needed that isn't in the filtered set, flag it as an issue rather than pulling from raw registries." Without this, an agent could bypass the Resolver's enum filtering by going back to the source. The whole point is they don't.