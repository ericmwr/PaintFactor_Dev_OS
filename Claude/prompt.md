Use the `AppFactory` orchestrator. Use the AppFactory Orchestrator agent to Find the root cause and resolutions to these issues. 

There are three things that we need to look for when addressing issues:

1. Is the issue at all with the specification?
2. Is the issue at all with the database?
3. Is the issue with the estimate engine itself?
Or number four, is it simply a UI issue? 

We must explore and answer these questions Before applying any Plans or fixes. All issues, fixes, and resolutions must be saved to the following memory file. C:\Users\mowre\.claude\projects\C--Eric-AI-Playground-Claude-Code-Uni\memory\substrate_state_bug_investigation.md To keep an accurate account of all changes. 

 Currently in the room editor, we have room-level overrides, and we have the quality tier, the height band, the complexity, and the surface app method. The quality tier is already set for the project, so we do not need it at the room level. We need it in the right-hand panel of the surface selection panel, where the details are. For example, walls: you select walls, and in the right-hand panel, you have substrate state, application method, and texture. There should also be the quality tier there, so that way in each paintable item selection, we have an override for that item. And that should be on every paintable item, but they'll still be affected by the project-level quality tier and only apply if overridden for that paintable item in particular. The height band at the room level is unnecessary. That should be a default. So if our height in the room, that should react to the height measurement for the room. So if we have a 20-foot tall height on the room, then the height band would take effect on all the tasks that have a height modifier for their spec. Complexity override can stay, but the surface application method, that should also be removed at the room level override because it's already in place for each paintable item selection. 